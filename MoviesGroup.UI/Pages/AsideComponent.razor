@* <aside id="aside-menu" class="d-flex flex-column m-2 ms-0 mt-0 p-2 pt-0 bg-body-secondary vh-100" style="min-width:160px; max-width:200px;">
    @foreach (var group in CaregoryLinkGroups)
    {
<LinkMainComponent LinkGroup="group"/>
    }
</aside>
 
@code {
    List<LinkGroup> CaregoryLinkGroups { get; set; } = new ()
    {
        new LinkGroup()
        {
            Name = "Category", 
            new List<LinkOption>() = new()
            {
                new LinkOption { AsideComponent = 1, Name = "Women", IsSelected = true },
                new LinkOption { AsideComponent = 2, Name = "Men", IsSelected = false },
                new LinkOption { AsideComponent = 3, Name = "Children", IsSelected = false }
            }
        }
    };
}

 *@